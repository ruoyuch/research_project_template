# output subfolder
Gitignored (only this README is tracked). Holds `figures/` and `tables/` that pipeline scripts produce and the draft consumes.
Everything here should be reproducible by re-running the pipeline — if it can't be regenerated, it belongs somewhere else.
The difference between things in `output/` is machine generated and in `docs/` is human generated.
